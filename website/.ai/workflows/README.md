# Workflows

Multi-step playbooks agents can follow for recurring tasks (e.g. release checklist, design pass).

Add one markdown file per workflow, kebab-case name (e.g. `ship-feature.md`). Keep steps concrete and ordered.
